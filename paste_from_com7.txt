(.venv) fibo07@fibo07:~/ioon/ioon-research$ .venv/bin/python3 scripts/diagnostics/pooled_student_check.py --ckpt wm/runs/beh12_ego/best.pt --data data/egocentric/beh12_c08f09t09_ego_flat --embodiment hexapod --cache results/wm/cache/ego_hex.pt --projector wm/runs/beh12_ego/projector_ego.pt --reference 0.608 0.847 && .venv/bin/python3 scripts/diagnostics/pooled_student_check.py --ckpt wm/runs/beh12_ego/best.pt --data data/egocentric/beh12_b1_ego_flat --embodiment b1 --cache results/wm/cache/ego_b1.pt --projector wm/runs/beh12_ego/projector_ego.pt --reference 0.334 0.778
Warning: You are sending unauthenticated requests to the HF Hub. Please set a HF_TOKEN to enable higher rate limits and faster downloads.
Loading weights: 100%|███████████████████████████████████████████████████████████████████████████████████████████████████████████| 843/843 [00:00<00:00, 17067.63it/s]
wm/runs/beh12_ego/best.pt
48 clips of hexapod from data/egocentric/beh12_c08f09t09_ego_flat
499 train / 499 test transitions, split by clip, pooled width 1408

                                features  action R2  within cond    alpha
                      e_t  (every token)      0.329        0.302      0.1
      pooled(e_t)  (the student's input)      0.308        0.279        1
  [pooled, goal]  (the Student's real input)      0.322        0.294        1
                        [pooled(e_t), z]      0.592        0.576        1
       [pooled, proj(a_t-1)]  (causal z)      0.869        0.863      0.1
  [pooled, a_t-1]  (the autoregression control)      0.914        0.910        1

                     pooled, Student MLP      0.263
                [pooled, z], Student MLP      0.634

                       like against like     pooled     full    kept
                             ridge, no z      0.308    0.329     93%
                           ridge, with z      0.592    0.608     97%
                       nonlinear, with z      0.634    0.847     75%

  what the student can actually be given           
               pooled alone, Student MLP      0.263
     [pooled, goal], the Student exactly      0.264

  **Neither pooled-alone nor [pooled, goal] is the bar.** Pooled-alone drops an
  input the student receives and reads too low; [pooled, goal] pooled over all
  twelve conditions is mostly behaviour SELECTION, which F145 already showed the
  pipeline does at 55% against 33%. **The `within cond` column above is the one a
  closed-loop result is read against** -- goal held constant, so it asks only
  whether the right MAGNITUDE was produced. A student high on the first and low on
  the second picks the right behaviour and cannot steer it, which fails the closed
  loop with a perfect teacher: the F145 wall, arriving at the student.
              + proj(a_t-1), Student MLP      0.785   +0.522
                + a_t-1 raw, the control      0.807   +0.544

  **Read the two causal rows against each other, not against pooled alone.**
  A gait is periodic, so a_t-1 nearly states a_t by itself; if the raw control
  lifts as much as proj(a_t-1) does, the lift is autoregression and says nothing
  about the latent. Only proj(a_t-1) clearly beating raw a_t-1 is evidence that
  feeding the student a causal z is worth the architecture change.

  **The comparison is against the same rows on the allocentric checkpoint, never
  against these alone.** The question is not what pooling costs -- it is whether it
  costs MORE egocentrically, which is what F174's spatial hypothesis predicts and what
  would mean a pooling student trains toward the linear floor instead of the refit
  reference. **If it does, the student needs the token grid and the 20 Hz budget has to
  be revisited before anything is built; no teacher quality repairs it.**
Warning: You are sending unauthenticated requests to the HF Hub. Please set a HF_TOKEN to enable higher rate limits and faster downloads.
Loading weights: 100%|███████████████████████████████████████████████████████████████████████████████████████████████████████████| 843/843 [00:00<00:00, 15528.80it/s]
wm/runs/beh12_ego/best.pt
48 clips of b1 from data/egocentric/beh12_b1_ego_flat
504 train / 504 test transitions, split by clip, pooled width 1408

                                features  action R2  within cond    alpha
                      e_t  (every token)      0.117        0.084      0.1
      pooled(e_t)  (the student's input)      0.067        0.032        1
  [pooled, goal]  (the Student's real input)      0.093        0.059        1
                        [pooled(e_t), z]      0.328        0.303        1
       [pooled, proj(a_t-1)]  (causal z)      0.477        0.458      0.1
  [pooled, a_t-1]  (the autoregression control)      0.681        0.669        1

                     pooled, Student MLP      0.081
                [pooled, z], Student MLP      0.419

                       like against like     pooled     full    kept
                             ridge, no z      0.067    0.117     57%
                           ridge, with z      0.328    0.334     98%
                       nonlinear, with z      0.419    0.778     54%

  what the student can actually be given           
               pooled alone, Student MLP      0.081
     [pooled, goal], the Student exactly      0.124

  **Neither pooled-alone nor [pooled, goal] is the bar.** Pooled-alone drops an
  input the student receives and reads too low; [pooled, goal] pooled over all
  twelve conditions is mostly behaviour SELECTION, which F145 already showed the
  pipeline does at 55% against 33%. **The `within cond` column above is the one a
  closed-loop result is read against** -- goal held constant, so it asks only
  whether the right MAGNITUDE was produced. A student high on the first and low on
  the second picks the right behaviour and cannot steer it, which fails the closed
  loop with a perfect teacher: the F145 wall, arriving at the student.
              + proj(a_t-1), Student MLP      0.482   +0.402
                + a_t-1 raw, the control      0.496   +0.415

  **Read the two causal rows against each other, not against pooled alone.**
  A gait is periodic, so a_t-1 nearly states a_t by itself; if the raw control
  lifts as much as proj(a_t-1) does, the lift is autoregression and says nothing
  about the latent. Only proj(a_t-1) clearly beating raw a_t-1 is evidence that
  feeding the student a causal z is worth the architecture change.

  **The comparison is against the same rows on the allocentric checkpoint, never
  against these alone.** The question is not what pooling costs -- it is whether it
  costs MORE egocentrically, which is what F174's spatial hypothesis predicts and what
  would mean a pooling student trains toward the linear floor instead of the refit
  reference. **If it does, the student needs the token grid and the 20 Hz budget has to
  be revisited before anything is built; no teacher quality repairs it.**