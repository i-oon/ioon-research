############ STEP 0 -- LOOK AT THE CAMERA BEFORE ANYTHING ELSE ############
Multiple -pix_fmt options specified for stream 0, only the last option '-pix_fmt yuv420p' will be used.
Multiple -pix_fmt options specified for stream 0, only the last option '-pix_fmt yuv420p' will be used.
  c10f10t10_ep0 66 frames -> results/ego_derisk/c10f10t10_ep0.mp4  (70 KB)
  grid overview -> results/ego_derisk/grid_overview.mp4  (64 KB)

H.264 videos in results/ego_derisk/

  **Watch results/ego_derisk/ now.** The frame must show the room from the robot's head, moving as it walks.
  If it shows the sky, the floor, or the robot's own body, fix --ego_euler / --ego_offset and
  rerun this step. Nothing below is meaningful until that frame is right.
  the direction is MEASURED, not assumed: insect from head-minus-abdomen, B1 from the
  direction the base actually travels. The camera sits on /head (insect) and on the base
  (B1), 3 cm ahead and 2 cm up. Override with --ego_forward / --ego_offset if the frame
  still looks wrong -- and say what it looks like, since that names the fault.

  does the frame look like a forward head view? [y/N]   stopping, as intended
